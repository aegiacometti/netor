# usage example: ansible-playbook win-user-delete-local.yml --extra-vars "user=user_name server=server"
---
- name: Win delete user playbook Local
  hosts: '{{ server }}'
  gather_facts: false
  tasks:

    - name: Check connection to Server
      wait_for_connection:
        connect_timeout: 3
        timeout: 5
      register: connection
      ignore_errors: yes

    - name: Win delete user Local
      win_user:
        name: '{{ user }}'
        state: absent
      register: output

    - debug:
        msg: '{{ output }}'